COLUMN TABLE AI_KNOWLEDGE (

    ID NVARCHAR(36) NOT NULL,
    SCENARIO NVARCHAR(100) NOT NULL,
    DESCRIPTION NCLOB,
    RESOLUTION NCLOB,
    EMBEDDING REAL_VECTOR(1536),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (ID)

);